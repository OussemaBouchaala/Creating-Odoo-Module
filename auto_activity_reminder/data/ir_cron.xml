<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="send_reminder_res_partner" model="ir.cron">
        <field name="name">Créer un appel avec les nouveaux contacts</field>
        <field name="model_id" ref="model_auto_activity_reminder"/>
        <field name="state">code</field>
        <field name="code">model.send_reminder()</field>
        <field name='interval_number'>1</field>
        <field name='interval_type'>days</field>
        <field name="numbercall">-1</field>
        <field name="nextcall"
               eval="(DateTime.now().replace(hour=8, minute=0, second=0, microsecond=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>

    </record>

</odoo>